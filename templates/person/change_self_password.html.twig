{% extends 'base.html.twig' %}
{% block title %}Change Password{% endblock %}
{% block body %}
    <h1>Change Password</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
    <a href="{{ path('dashboard') }}"><button>Cancel</button></a>
{% endblock %}
