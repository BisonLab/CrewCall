{% extends 'base.html.twig' %}
{% block title %}New Function{% endblock %}
{% block body %}
    <h1>New Function</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn-sm btn-dark" type="submit" value="Create" />
    {{ form_end(form) }}
    <a href="{{ path('function_index') }}" class="btn-sm btn-dark">Cancel</a>

{% endblock %}
