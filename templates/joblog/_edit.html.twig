{% form_theme edit_form 'form_div_layout.html.twig' %}
{{ form_start(edit_form, {action:path('joblog_edit', {'access': 'ajax', 'joblog': joblog.id}), 'attr': {'id':'editJobLogForm'}}) }}
    {{ form_widget(edit_form) }}
    <input class="btn-sm btn-dark" id="submitEditJobLog" type="submit" value="Save" />
{{ form_end(edit_form) }}
