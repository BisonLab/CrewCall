{% form_theme form 'form_div_layout.html.twig' %}
{% if shift %}
    {{ form_start(form, {action:path('joblog_create', {'access': 'ajax', 'shift': shift.id }), 'attr': {'id':'newJobLogForm'}}) }}
{% else %}
    {{ form_start(form, {action:path('joblog_create', {'access': 'ajax', 'job': job.id }), 'attr': {'id':'newJobLogForm'}}) }}
{% endif %}
    {{ form_widget(form) }}
    <input id="submitNewJobLog" type="submit" value="Add" />
{{ form_end(form) }}
